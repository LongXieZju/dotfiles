# Unix
alias ll="ls -al"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias clear="clear;ls"
function mycd(){builtin cd "$@" && ls}
alias cd=mycd

# Git
alias gs="git status"
alias gcm="git commit -am"
alias gpl="git pull"
alias gps="git push"
alias gco="git checkout"
